import React from 'react';
import logo from '../assets/images/logo/icon-left-font.png';
/* Header */

export default (props) => {
	return (
		<div className="header">
		<img src={logo} className="header__title"/>
		</div>
		);
};